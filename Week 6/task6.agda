
-- with - can only on high level, cuz deptiped
-- f p x == f p x with px
            -- f p x | true
            -- ... | false -- syntax

-- with can be replaced with a helper, as alwis

-- case needs to be able to change types in type level
-- in 'where' we have a more abstract statement
-- 'with' does all the substs!


-- but how to pattern match an intermediate value? see further

-- filter with adds an arg
-- ... - belongs to first guy

























--
